// Versions come from settings.gradle.kts (pluginManagement), which can read
// gradle.properties; a plugins block here only accepts constant versions.
plugins {
    id("com.android.application") apply false
    id("org.jetbrains.kotlin.android") apply false
    id("com.chaquo.python") apply false
}
