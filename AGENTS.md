# Halo_Serdes 协作规则

> 本项目使用 Project Cairn:`AGENTS.md` 是规则与导航入口,`cairn/` 是项目知识/状态层。同目录 `CLAUDE.md` 只有一行 `@AGENTS.md`,使 Claude Code 读到同一份规则;Codex 直接读本文件。

## 项目一句话

行为级高速 SerDes 仿真框架:NRZ ≤32 Gb/s 与 PAM4 ≤224 Gb/s,mixed-signal 与 ADC-based 双接收机架构可公平对比,时域 MC 与 StatEye 统计双引擎交叉校验。

## Init 配置

- 毕业 provider:暂缓对接(deferred —— 首次毕业时再接知识库)
- 知识库索引 / 毕业目标:尚未配置

## 进入项目后的阅读顺序

1. 先读本文件(AGENTS.md)。
2. 读根目录 `ROADMAP.md`(非 `cairn/`):待办与优先级,每条附证据。
3. 读 `cairn/LOG.md` 最新几条(新条目在顶部):近期进展与关键决策。
4. 按任务需要读 `cairn/` 下相关知识专题文档。

## 文档职责

| 文件 | 角色 | 维护方式 |
|---|---|---|
| `AGENTS.md`(根) | 规则与导航 | 很少变,≤ 65 行(模板默认 60,本项目为 6 条铁律留出余量) |
| `CLAUDE.md`(根) | 单行 `@AGENTS.md` 存根 | 写一次,不再改动 |
| `ROADMAP.md`(根) | 待办与优先级 | 就地更新;完成即删除并在 LOG 记一笔 |
| `CHANGELOG.md`(根) | 已交付的演进 | 按里程碑追加 |
| `cairn/LOG.md` | 时间序日志 | 新条目加在顶部,每条 ≤ 20 行,只写摘要+指针 |
| `cairn/<topic>.md` | 知识专题文档(当前真相) | 就地更新;坑放在正文小节并用 `contains` 标注 |
| `cairn/Reference/` | 外部原始输入 | 按需创建,只追加 |
| `cairn/Cited.md` | 知识库引用清单 | 只放指针,不复制原文 |

> 其余文件只在出现具体信号时才创建,不预建空壳。工程资产(被代码或流程消费的配置/契约/规格)留在代码树里。面向使用者的文档(`README.md`、`docs/`)与面向协作者的知识(`cairn/`)读者不同,不要互相搬运。

## 项目铁律(改动时不要破坏;论证与约定见 `cairn/architecture-invariants.md`)

1. **配置是唯一真相源** —— 一切由 frozen `LinkConfig` 决定,无全局状态。
2. **双 RX 架构必须公平可比** —— 差异只允许在两个组装函数与各自专属模块内。
3. **双引擎交叉校验** —— 纯 LTI + AWGN 下统计与时域必须在 2× 内吻合。
4. **numba 是性能层不是正确性层** —— `HALO_NO_JIT=1` 必须给出一致结果。
5. **波形域与符号域不能隐式互转** —— 只能经显式采样器。
6. **桌面与 Android 必须同时验证** —— 动了共用层就两端都要跑通,任一端未验证即未完成。

开工前先读 `cairn/engineering-pitfalls.md`(踩过并付出过代价的坑)。

## 冲突仲裁

- 优先级:**知识专题文档 > LOG 历史**;结论以 `cairn/` 专题文档的最新记录为准,不以更早的 LOG 条目为准。规则层面的冲突由本文件裁定。

## 知识库消费反射

- 若某项工作的可复用内核(它产出或依赖的任何结论)够格毕业,先查阅本项目自己的 `cairn/` 知识专题文档;当前尚未对接外部知识库(provider 暂缓对接 —— 见上方 Init 配置),外部索引检查与 `cairn/Cited.md` 引用在接入后才启用。

## 文档协作规则

- 动手前先判断用户要的是"讨论/建议"还是"直接改文档";说"先看看/先评估"时先给分析,不要直接重写正式文档。
- 修正过去的判断时追加更正说明,不要静默覆盖。
- 未经确认的判断不要写成既定事实。

## 知识沉淀规则

- 每次实质性推进后,在 `cairn/LOG.md` 顶部加一条(摘要+指针);结论沉淀进 `cairn/` 专题文档。
- **完成回复闸门**:在任何完成类声明之前(工作已完成/已实现/已定稿/已更新/已同步/已验证/测试通过、问题已修复/已解决、交付物可用、工作已结束,及语义等价表述),先执行 `references/maintenance.md` 的 Cairn 检查点,只更新其触发矩阵要求的记录并核验,再回复。明确的只读/不要改动要求禁止 Cairn 写入。
- 跨项目可复用的经验经毕业机制沉淀 —— 待接入知识库后启用(provider 暂缓对接,见上方 Init 配置)。
